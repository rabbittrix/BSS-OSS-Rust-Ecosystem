apiVersion: v1
kind: Service
metadata:
  name: { { include "bss-oss-rust.fullname" . } }
  labels: { { - include "bss-oss-rust.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "bss-oss-rust.selectorLabels" . | nindent 4 } }
